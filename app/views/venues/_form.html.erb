<%= form_with(model: venue) do |form| %>
  <% if venue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(venue.errors.count, "error") %> prohibited this venue from being saved:</h2>

      <ul>
        <% venue.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="input-container">
    <%= form.label :name %>
    <%= form.text_field :name, class: "form-control", autofocus: true %>
  </div>
  <div class="input-container">
    <%= form.label :description %>
    <%= form.text_area :description, class: "form-control", autofocus: true %>
  </div>
  <div class="input-container">
    <%= form.label :location %>
    <%= form.text_field :location, class: "form-control", autofocus: true %>
  </div>

  <div class="flex justify-end mt-4">
    <%= link_to 'Cancel', venues_path %>
    <%= form.submit (), class: "btn btn-primary ml-3" %>
  </div>
<% end %>
